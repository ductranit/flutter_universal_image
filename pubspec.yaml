name: universal_image
description: A flutter package to display all image types for all platforms
version: 0.1.4
homepage: https://github.com/ductranit/flutter_universal_image
repository: https://github.com/ductranit/flutter_universal_image
issue_tracker: https://github.com/ductranit/flutter_universal_image/issues

environment:
  sdk: '>=2.14.0 <3.0.0'
  flutter: '>=2.5.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.0
  extended_image: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
